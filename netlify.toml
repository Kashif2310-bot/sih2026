[build]
  command = "npm run build"
  publish = "dist"

# react-router uses client-side (BrowserRouter) routing, so every path must
# fall back to index.html or a direct link to e.g. /scan or /export would 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
